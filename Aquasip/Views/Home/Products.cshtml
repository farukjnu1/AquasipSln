@{

    ViewData["Title"] = "Products | AquaSip";

    var viewModel = (IEnumerable<Aquasip.Models.PageVM>)ViewData["aquasip"];

    var layoutPage = viewModel.Where(x => x.Slug == "layout").FirstOrDefault() == null ? new PageVM()
        : viewModel.Where(x => x.Slug == "layout").First();
    // var oPhone = layoutPage.PageContents.Where(x => x.SlugPageContent == "phone").FirstOrDefault() == null ? new Aquasip.Models.PageContentVM()
    //     : layoutPage.PageContents.Where(x => x.SlugPageContent == "phone").First();

    var productPage = viewModel.Where(x => x.Slug == "products").FirstOrDefault();
    if (productPage == null)
    {
        productPage = new PageVM()
        {
            Title = "Products",
            Slug = "products"
        };
    }
    
    #region Products
    var listProduct = productPage.Products;
    if (listProduct.Count == 0)
    {
        listProduct.Add(new ProductVM()
        {
            ProductId = 0,
            ProductCode = "STL101",
            ProductName = "Insulated Steel Bottle (With Grip/Sleeve)",
            Price = 1099,
            ListProductMedia = new List<ProductMediumVM>() { new ProductMediumVM() { FilePath = "/img/steel_bottle_grip.png", } }
        });
        listProduct.Add(new ProductVM()
        {
            ProductId = 0,
            ProductCode = "STL102",
            ProductName = "Insulated Steel Bottle(With Grip / Sleeve)",
            Price = 999,
            ListProductMedia = new List<ProductMediumVM>() { new ProductMediumVM() { FilePath = "/img/steel_bottle.png", } }
        });
        listProduct.Add(new ProductVM()
        {
            ProductId = 0,
            ProductCode = "SL201",
            ProductName = "Silicone Water Bottle",
            Price = 850,
            ListProductMedia = new List<ProductMediumVM>() { new ProductMediumVM() { FilePath = "/img/silicone_bottle.png", } }
        });
    }
    #endregion

    var oNavbar = new Aquasip.Models.NavbarVM()
    {
        Name = productPage.Title,
        Slug = productPage.Slug,
        Pages = (List<PageVM>)viewModel
    };

}

<!-- Navbar & Hero Start -->
<partial name="_NavbarTop" model="oNavbar" />
<!-- Navbar & Hero End -->

<!-- Products Start -->
<div class="container-fluid product py-2">
    <div class="container py-2">
        <div class="text-center mx-auto pb-5 wow fadeInUp" data-wow-delay="0.2s" style="max-width: 800px;">
            <h4 class="text-uppercase text-primary">Our Products</h4>
            <h1 class="display-3 text-capitalize mb-3">We Deliver Best Quality Bottle Packs.</h1>
        </div>
        <div class="row g-4 justify-content-center">
            @for (var i = 0; i < listProduct.Count; i++)
            {
                var item = listProduct[i];
                int dela = (i + 1) * 20;
                double delas = dela / 100.0;
                string delays = delas.ToString() + "s";
                string FilePath = item.ListProductMedia.Count > 0 ? item.ListProductMedia[0].FilePath : "";
                <div class="col-lg-6 col-xl-4 wow fadeInUp" data-wow-delay="@delays">
                    <div class="product-item">
                        <img src="@FilePath" class="img-fluid w-100 rounded-top" alt="Image" />
                        <div class="product-content bg-light text-center rounded-bottom p-4">
                            <p>@item.ProductCode</p>
                            <a href="#" class="h4 d-inline-block mb-3">@item.ProductName</a>
                            <p class="fs-4 text-primary mb-3">BDT. @item.Price</p>
                            <a href="/Home/Spec?id=@item.ProductId" class="btn btn-secondary rounded-pill py-2 px-4 m-1">Spec</a>
                            <a href="/Home/Spec?id=@item.ProductId" class="btn btn-primary rounded-pill py-2 px-4 m-1">Order Now</a>
                            <a href="/Home/Review?id=@item.ProductId" class="btn btn-warning rounded-pill py-2 px-4 m-1">Leave Review</a>
                        </div>
                        <div class="product-content bg-light text-center rounded-bottom p-4">
                            @if (item.AverageRating < 2)
                            {
                                var starts = "Review: ⭐";
                                @starts
                            }
                            else if (item.AverageRating < 3)
                            {
                                var starts = "Review: ⭐⭐";
                                @starts
                            }
                            else if (item.AverageRating < 4)
                            {
                                var starts = "Review: ⭐⭐⭐";
                                @starts
                            }
                            else if (item.AverageRating < 5)
                            {
                                var starts = "Review: ⭐⭐⭐⭐";
                                @starts
                            }
                            else if (item.AverageRating < 6)
                            {
                                var starts = "Review: ⭐⭐⭐⭐⭐";
                                @starts
                            }

                        </div>
                    </div>
                </div>
            }

        </div>
    </div>
</div>
<!-- Products End -->