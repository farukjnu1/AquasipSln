@model Aquasip.Models.CustomerVM

@{
    ViewData["Title"] = "Sign-in";
    Layout = "~/Views/Shared/_Layout.cshtml";

    var viewModel = (IEnumerable<Aquasip.Models.PageVM>)ViewData["aquasip"];

    var layoutPage = viewModel.Where(x => x.Slug == "layout").FirstOrDefault() == null ? new PageVM()
        : viewModel.Where(x => x.Slug == "layout").First();

    // var oPhone = layoutPage.PageContents.Where(x => x.SlugPageContent == "phone").FirstOrDefault() == null ? new Aquasip.Models.PageContentVM()
    //     : layoutPage.PageContents.Where(x => x.SlugPageContent == "phone").First();

    var homePage = viewModel.Where(x => x.Slug == "home").FirstOrDefault() == null ? new PageVM()
        : viewModel.Where(x => x.Slug == "home").First();
    //var oMedia0 = Model.ListProductMedia.Count > 0 ? Model.ListProductMedia[0] : new ProductMediumVM();

    var oNavbar = new Aquasip.Models.NavbarVM()
    {
        Name = homePage.Title,
        Slug = homePage.Slug,
        Pages = (List<PageVM>)viewModel
    };
}
<!-- Navbar & Hero Start -->
<partial name="_NavbarTop" model="oNavbar" />
<!-- Navbar & Hero End -->
<div class="container">
    <h1>Sign-in</h1>

    <h4>Customer</h4>
    <hr />
    <div class="row">
        <div class="col-md-4">
            <form asp-action="Signin">
                <input type="hidden" asp-for="CallbackController" />
                <input type="hidden" asp-for="CallbackAction" />
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="Email" class="control-label"></label>
                    <input type="email" asp-for="Email" class="form-control" />
                    <span asp-validation-for="Email" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="PasswordHash" class="control-label"></label>
                    <input type="password" asp-for="PasswordHash" class="form-control" />
                    <span asp-validation-for="PasswordHash" class="text-danger"></span>
                </div>
                <div class="form-group pt-2">
                    <input type="submit" value="Ok" class="btn btn-primary" />
                    Don't have an account? <a asp-action="Signup">Sign-up</a>
                </div>
            </form>
            <div id="EmailVerify" style="display:none">
                <form asp-action="RequestEmailVerify">
                    <div class="form-group">
                        <label asp-for="Email" class="control-label"></label>
                        <input type="email" asp-for="Email" class="form-control" />
                        <span asp-validation-for="Email" class="text-danger"></span>
                    </div>
                    <div class="form-group pt-2">
                        <input type="submit" value="Request Email Verification" class="btn btn-primary" />
                        <a href="javascript:void(0)" onclick="hideEmailVerify()">Close Email Verification</a>
                    </div>
                </form>
            </div>
            <div id="PasswordReset" style="display:none">
                <form asp-action="RequestPasswordReset">
                    <div class="form-group">
                        <label asp-for="Email" class="control-label"></label>
                        <input type="email" asp-for="Email" class="form-control" />
                        <span asp-validation-for="Email" class="text-danger"></span>
                    </div>
                    <div class="form-group pt-2">
                        <input type="submit" value="Request Password Reset" class="btn btn-primary" />
                        <a href="javascript:void(0)" onclick="hidePasswordReset()">Close Password Reset</a>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <div>
        Verified Account? <a href="javascript:void(0)" onclick="showEmailVerify()">Request Verification</a>
    </div>
    <div>
        Forget Password? <a href="javascript:void(0)" onclick="showPasswordReset()">Request Reset Password</a>
    </div>

</div>

@* @section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
} *@

<script>
    
    function documentReady(){
        $(document).ready(function () {
        });
    }

    function showEmailVerify(){
        $('#EmailVerify').show();
    }

    function hideEmailVerify(){
        $('#EmailVerify').hide();
    }

    function showPasswordReset(){
        $('#PasswordReset').show();
    }

    function hidePasswordReset(){
        $('#PasswordReset').hide();
    }

    setTimeout(() => {
        console.log("This runs after 1 seconds");
        documentReady();
    }, 1000); // 10000 ms = 10 seconds
</script>
