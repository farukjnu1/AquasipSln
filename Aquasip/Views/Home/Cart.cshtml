@model IEnumerable<Aquasip.Models.ProductVM>
@{
    ViewData["Title"] = "AquaSip | Cart";
    // Layout = "~/Views/Shared/_Layout.cshtml";

    var viewModel = (IEnumerable<Aquasip.Models.PageVM>)ViewData["layout"];

    var layoutPage = viewModel.Where(x => x.Slug == "layout").FirstOrDefault() == null ? new PageVM() 
    : viewModel.Where(x => x.Slug == "layout").First();

    var oPhone = layoutPage.PageContents.Where(x => x.IsActive == true && x.SlugPageContent == "phone").FirstOrDefault() == null ? new Aquasip.Models.PageContentVM()
    : layoutPage.PageContents.Where(x => x.IsActive == true && x.SlugPageContent == "phone").First();

    var oLogo = layoutPage.PageContents.Where(x => x.IsActive == true && x.SlugPageContent == "logo").FirstOrDefault() == null ? new Aquasip.Models.PageContentVM()
     : layoutPage.PageContents.Where(x => x.IsActive == true && x.SlugPageContent == "logo").First();

    // var oMedia0 = Model.ListProductMedia.Count > 0 ? Model.ListProductMedia[0] : new ProductMediumVM();

    var cartPage = viewModel.Where(x => x.Slug == "cart").FirstOrDefault() == null ? new PageVM() 
        : viewModel.Where(x => x.Slug == "cart").First();

    var delivery_charge = cartPage.PageContents.Where(x => x.IsActive == true && x.SlugPageContent == "delivery_charge").FirstOrDefault() == null ? new Aquasip.Models.PageContentVM()
        : cartPage.PageContents.Where(x => x.IsActive == true && x.SlugPageContent == "delivery_charge").First();
    var gateway_charge = cartPage.PageContents.Where(x => x.IsActive == true && x.SlugPageContent == "gateway_charge").FirstOrDefault() == null ? new Aquasip.Models.PageContentVM()
        : cartPage.PageContents.Where(x => x.IsActive == true && x.SlugPageContent == "gateway_charge").First();
    var vat = cartPage.PageContents.Where(x => x.IsActive==true && x.SlugPageContent == "vat").FirstOrDefault() == null ? new Aquasip.Models.PageContentVM()
    : cartPage.PageContents.Where(x => x.IsActive == true && x.SlugPageContent == "vat").First();
    decimal? grandTotal = 0;
    decimal? subTotal = 0;
    foreach (var item in Model)
    {
        subTotal += item.Total == null ? 0 : item.Total;
    }
    grandTotal += subTotal;
    if (grandTotal > 0)
    {
        if (vat.IsActive == true)
        {
            grandTotal += subTotal * (Convert.ToDecimal(vat.Header) / 100);
        }
        if (delivery_charge.IsActive == true)
        {
            grandTotal += Convert.ToDecimal(delivery_charge.Header);

        }
        if (gateway_charge.IsActive == true)
        {
            grandTotal += Convert.ToDecimal(gateway_charge.Header);
        }
        grandTotal = Math.Round(Convert.ToDecimal(grandTotal), 2);
    }
}
<style>
    /* cart page additions */
    .cart-header {
        margin-bottom: 24px;
    }

        .cart-header h1 {
            margin: 0 0 8px;
            font-size: 2rem;
        }

        .cart-header p {
            margin: 0;
            color: #555;
        }

    .cart-content {
        display: grid;
        grid-template-columns: 1.5fr 0.6fr;
        gap: 24px;
    }

    .cart-items {
        background: #ffffff;
        border-radius: 16px;
        padding: 24px;
        box-shadow: 0 12px 30px rgba(0, 0, 0, 0.08);
    }

    .cart-item {
        display: grid;
        grid-template-columns: 3fr 1fr 1fr 1fr;
        align-items: center;
        gap: 16px;
        padding: 18px 0;
        border-bottom: 1px solid #ececec;
    }

        .cart-item:last-of-type {
            border-bottom: none;
        }

    .cart-item-header {
        font-weight: 700;
        color: #4f46e5;
        border-bottom: 2px solid #ececec;
        padding-bottom: 14px;
    }

    .item-product {
        display: flex;
        gap: 16px;
        align-items: center;
    }

        .item-product img {
            width: 90px;
            height: 90px;
            border-radius: 16px;
            object-fit: cover;
        }

        .item-product h2 {
            margin: 0 0 6px;
            font-size: 1rem;
        }

        .item-product p {
            margin: 0;
            color: #666;
            font-size: 0.95rem;
        }

    .item-price,
    .item-total {
        font-weight: 600;
        color: #1d1d1d;
    }

    .item-qty input {
        width: 72px;
        padding: 10px;
        border: 1px solid #d1d5db;
        border-radius: 10px;
        text-align: center;
    }

    .cart-actions {
        display: flex;
        justify-content: space-between;
        gap: 12px;
        margin-top: 24px;
    }

    .order-summary {
        background: #ffffff;
        border-radius: 16px;
        padding: 24px;
        box-shadow: 0 12px 30px rgba(0, 0, 0, 0.08);
    }

        .order-summary h2 {
            margin-top: 0;
        }

    .summary-row,
    .summary-total {
        display: flex;
        justify-content: space-between;
        margin-bottom: 14px;
        font-size: 1rem;
    }

    .summary-total {
        margin-top: 16px;
        font-weight: 700;
        font-size: 1.1rem;
        color: #1d1d1d;
    }

    .checkout-btn,
    .secondary-btn,
    .danger-btn {
        width: 100%;
        padding: 14px 18px;
        border: none;
        border-radius: 12px;
        font-size: 1rem;
        cursor: pointer;
    }

    .checkout-btn {
        background: #4f46e5;
        color: #fff;
        margin-top: 18px;
    }

    .secondary-btn {
        background: #eef2ff;
        color: #4f46e5;
    }

    .danger-btn {
        background: #fee2e2;
        color: #b91c1c;
    }

    @@media (max-width: 960px) {
        .cart-content

    {
        grid-template-columns: 1fr;
    }

    }

    @@media (max-width: 680px) {
        .cart-item

    {
        grid-template-columns: 1fr;
        gap: 12px;
    }

    .cart-item-header {
        grid-template-columns: 1fr;
    }

    .cart-actions {
        flex-direction: column;
    }

    }
</style>

<!-- Navbar & Hero Start -->
<div class="container-fluid position-relative p-0">
    <nav class="navbar navbar-expand-lg navbar-light px-4 px-lg-5 py-3 py-lg-0">
        <a href="/" class="navbar-brand p-0">
            @if (oLogo.IsActive == true)
            {
                <h1 class="text-primary m-0"><img src="@oLogo.FilePath" /></h1>
            }
            else
            {
                <h1 class="text-primary m-0">YOUR-LOGO</h1>
            }
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
            <span class="fa fa-bars"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <div class="navbar-nav ms-auto py-0">
                <a href="/" class="nav-item nav-link">Home</a>
                <a href="/Home/Products" class="nav-item nav-link">Products</a>
                <a href="javascript:void(0);" class="nav-item nav-link">Specification</a>
                <a href="/Home/Cart" class="nav-item nav-link active">Shopping Cart</a>
                <a href="/Home/Gallery" class="nav-item nav-link">Gallery</a>
                <a href="/Home/Contact" class="nav-item nav-link">Contact</a>
                <a href="/Home/Review" class="nav-item nav-link">Review</a>
                <a href="/Home/Faq" class="nav-item nav-link">FAQ</a>
            </div>
            <div class="d-none d-xl-flex me-3">
                <div class="d-flex flex-column pe-3 border-end border-primary">
                    <span class="text-body">Get Free Delivery</span>
                    <a href="tel:@oPhone.Header"><span class="text-primary">Free: @oPhone.Header</span></a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Header Start -->
    <div class="container-fluid bg-breadcrumb">
        <div class="container text-center py-5" style="max-width: 900px;">
            <h4 class="text-white display-4 mb-4 wow fadeInDown" data-wow-delay="0.1s">Shopping Cart</h4>
            <ol class="breadcrumb d-flex justify-content-center mb-0 wow fadeInDown" data-wow-delay="0.3s">
                <li class="breadcrumb-item"><a href="/">Home</a></li>
                <li class="breadcrumb-item active text-primary">Cart</li>
            </ol>
        </div>
    </div>
    <!-- Header End -->
</div>
<!-- Navbar & Hero End -->

<!-- Contact Start -->
<div class="container-fluid contact bg-light py-5">
    <div class="container py-5">

        <main class="cart-page">
            <header class="cart-header">
                <h1>Shopping Cart</h1>
                <p>Review your items, update quantities, or continue shopping.</p>
            </header>

            <section class="cart-content">
                <div class="cart-items">
                    <div class="cart-item cart-item-header">
                        <span>Product</span>
                        <span>Price</span>
                        <span>Qty</span>
                        <span>Total</span>
                        <span>#</span>
                    </div>

                    @foreach (var item in Model)
                    {
                        string FilePath = item.ListProductMedia.Count > 0 ? item.ListProductMedia[0].FilePath : "";

                        <div class="cart-item">
                            <div class="item-product">
                                <img src="@FilePath" alt="Product 1" />
                                <div>
                                    <h2>@item.ProductName</h2>
                                    <p>@item.ProductCode</p>
                                </div>
                            </div>
                            <div class="item-price">@item.Price</div>
                            <div class="item-qty">
                                <input type="number" min="1" value="@item.Quantity" readonly />
                            </div>
                            <div class="item-total">@item.Total</div>
                            <div class="item-remove">
                                <button class="remove-btn" onclick="clearCart(@item.ProductId)" title="Remove from cart">Remove</button>
                            </div>
                        </div>
                    }

                    @if (Model.Count() == 0)
                    {
                        <h5 style="text-align:center">Cart is empty!</h5>
                    }

                    <div class="cart-actions">
                        <button class="secondary-btn" onclick="goToProducts()">Continue Shopping</button>
                        <button class="danger-btn" onclick="clearAllCart()">Clear Cart</button>
                    </div>
                </div>

                <aside class="order-summary">
                    <h2>Order Summary</h2>
                    <div class="summary-row">
                        <span>Subtotal</span>
                        <span>@subTotal</span>
                    </div>
                    @if (vat.IsActive == true)
                    {
                        <div class="summary-row">
                            <span>@vat.Title</span>
                            <span>@vat.Header</span>
                        </div>
                    }
                    @if (delivery_charge.IsActive == true)
                    {
                        <div class="summary-row">
                            <span>@delivery_charge.Title</span>
                            <span>@delivery_charge.Header</span>
                        </div>
                    }
                    @if (gateway_charge.IsActive == true)
                    {
                        <div class="summary-row">
                            <span>@gateway_charge.Title</span>
                            <span>@gateway_charge.Header</span>
                        </div>
                    }
                    
                    <div class="summary-total">
                        <span>Total</span>
                        <span>@grandTotal</span>
                    </div>
                    <button class="checkout-btn" onclick="gotToCheckout()">Proceed to Checkout</button>
                </aside>
            </section>
        </main>
    </div>
</div>
<!-- Contact End -->

<script>
    function clearAllCart(){
        location.href='/Home/CartClearAll';
    }

    function clearCart(id){
        location.href='/Home/CartClear?id='+id;
    }

    function goToProducts(){
        location.href='/Home/Products';
    }

    function gotToCheckout(){
        location.href='/Home/Checkout';
    }
</script>

<!-- JavaScript Libraries -->
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.4/jquery.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js"></script>

<!-- Template Javascript -->
<script src="~/lib/wow/wow.min.js"></script>
<script src="~/lib/easing/easing.min.js"></script>
<script src="~/lib/waypoints/waypoints.min.js"></script>
<script src="~/lib/owlcarousel/owl.carousel.min.js"></script>
<script src="~/js/main.js"></script>